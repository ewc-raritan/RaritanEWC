document.addEventListener("DOMContentLoaded", () => {
    const e = document.querySelector(".amot-image"), t = document.querySelector(".amot-image-error");
    e && t && e.addEventListener("error", () => {
        e.closest(".amot-image-frame")?.setAttribute("hidden", ""), t.hidden = !1
    })
});